package j11_MethodCreeation;

import java.util.Scanner;

/*public class Task03 {
   public static void main(String[] args) {
        //TASK->GİRİLEN iki sayıyı seçilen 4 işleme göre hesaplayan method create ediniz..
        Scanner sc = new Scanner(System.in);
        System.out.println("birinci sayı giriniz : ");
        int sayi1 = sc.nextInt();

        System.out.println("ikinci sayı giriniz : ");
        int sayi2 = sc.nextInt();
        System.out.println("işleminizi giriniz");
        char islem = sc.next().charAt(0);
        islemMenu(islem, sayi1,sayi2);

    }//main sonu

    public static void islemMenu(char ch, int x, int y) {
        switch (ch) {
            case '+':
                topla(x, y);
                break;
            case '-':
                cıkar(x, y);
                break;
            case '*':
                carp(x, y);
                break;
            case '/':
                böl(x, y);
                break;
            default:
                System.out.println("hatalı giriş yaptını");

        }


    }


    private static void topla(int a, int b) {
        System.out.println("a+b= " + (a + b));
    }

    private static void cıkar(int a, int b) {
        System.out.println("a-b= " + (a - b));
    }

    private static void carp(int a, int b) {
        System.out.println("a*b= " + (a * b));
    }

    private static void böl(int a, int b) {
        System.out.println("a/b= " + (a / b));
    }


}//class sonu*/
